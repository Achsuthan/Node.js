var url = "www.google.com"

var callUrl = function(message) {
    //call the url for external login
    console.log(`This is a api call which is contain the message of ${message}`);
}

module.exports.endPoint = callUrl;